import { Button } from "@/components/ui/button";

const page = () => {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <p>page</p>
      <Button>Click</Button>
    </div>
  );
};

export default page;
